{
  "name": "mournwood",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mournwood — a dark-arcane roguelite card adventure (PWA, vanilla JS, no build step).",
  "scripts": {
    "test": "node --test test/",
    "fuzz": "node test/fuzzer.mjs"
  }
}
